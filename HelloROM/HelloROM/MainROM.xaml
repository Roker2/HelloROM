<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HelloROM"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             x:Class="HelloROM.MainROM">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center">
            <ffimageloading:CachedImage x:Name="ROMPic" WidthRequest="100" HeightRequest="100">
                <ffimageloading:CachedImage.Transformations>
                    <fftransformations:CircleTransformation/>
                </ffimageloading:CachedImage.Transformations>
            </ffimageloading:CachedImage>
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="-10">
                <Button Text="Download" BackgroundColor="White"/>
                <Button Text="Screenshots" BackgroundColor="White"/>
                <Button Text="About project" BackgroundColor="White"/>
                <Button Text="Site" BackgroundColor="White"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>