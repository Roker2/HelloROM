<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HelloROM"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="HelloROM.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="@strings/mainpagestring"
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
        <ListView x:Name="ROMList" HasUnevenRows="True" ItemTapped="ROMList_ItemTapped" ItemSelected="ROMList_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <!-- <StackLayout HorizontalOptions="Start"> -->
                            <!-- <ffimageloading:CachedImage Source ="{Binding Image}" WidthRequest="30" HeightRequest="30"/> -->
                                <!-- </StackLayout> -->
                            <Label Text="{Binding Path = Name}" FontSize="17"/>
                            <Label Text="{Binding Path = Base}" FontSize="9"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
